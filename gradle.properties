# Ensure AndroidX/Jetifier are enabled for Compose and Material dependencies
android.useAndroidX=true
android.enableJetifier=true

# Improve build stability in CI/desktop environments
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
