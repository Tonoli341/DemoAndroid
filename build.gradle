buildscript {
    repositories {
        google()
        mavenCentral()
    }
}

plugins {
    id("com.android.application") version "8.2.2" apply false
    // Align with Compose Compiler 1.5.4 (requires Kotlin 1.9.20)
    id("org.jetbrains.kotlin.android") version "1.9.20" apply false
}
